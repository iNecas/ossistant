<h1>Listing execution plans</h1>

<table>
  <tr>
    <th>Status</th>
    <th></th>
  </tr>

<% @plans.each do |plan| %>
  <tr>
    <th><%= h(plan.status) %></th>
    <td><a href="<%= h(url("/#{plan.persistence_id}")) %>">Show</a></td>
  </tr>
<% end %>
</table>

